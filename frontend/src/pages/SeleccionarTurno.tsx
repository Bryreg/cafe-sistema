// Reemplazada por GestionTurno en el nuevo flujo de turnos.
import { Navigate } from 'react-router-dom'
export default function SeleccionarTurno() {
  return <Navigate to="/gestion-turno" replace />
}
